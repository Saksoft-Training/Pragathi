import { HighlightMovie } from './highlight-movie';

describe('HighlightMovie', () => {
  it('should create an instance', () => {
    const directive = new HighlightMovie();
    expect(directive).toBeTruthy();
  });
});
